using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//update speed pulling from road speed (tbd on how to do this)
//create fake distance by adding 1mi every [x] seconds
//point value based on how long player lasts - 1pt per 1/2 second??

public class inGameUIManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
